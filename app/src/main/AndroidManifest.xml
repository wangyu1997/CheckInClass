<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.gstar_info.lab.com.checkinclass">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".clip.ClipHeaderActivity"/>
        <activity android:name=".MakeupinfoActivity"/>
        <activity android:name=".upHeaderActivity"/>
        <activity android:name=".Regist1Activity"/>
        <activity android:name=".Regist2Activity"/>
        <activity android:name=".AcademySelectActivity"/>
        <activity android:name=".MajorSelectActivity"/>
        <activity android:name=".ForgetPassActivity"/>

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".HomePageActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme">

        </activity>
        <activity
            android:name=".InfoActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme">

        </activity>

        <activity
            android:name=".PersonActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme">

        </activity>
    </application>

</manifest>